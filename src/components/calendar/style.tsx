import styled from 'styled-components'

export const CalendarWrapper = styled.div`
  &.calendar-wrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 5px 0;

    .calendar {
      width: 250px;
    }
  }
`
